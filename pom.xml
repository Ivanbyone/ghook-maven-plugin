<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.ivanbyone</groupId>
  <artifactId>ghook-maven-plugin</artifactId>
  <version>1.0.0-alpha-1</version>
  <packaging>maven-plugin</packaging>

  <name>Ghook Maven Plugin</name>
  <description>Useful plugin, for installing git hooks locally</description>
  <url>https://github.com/Ivanbyone/ghook-maven-plugin</url>

  <!-- Project license -->
  <licenses>
    <license>
      <name>MIT</name>
      <url>https://opensource.org/license/mit</url>
    </license>
  </licenses>

  <!-- Project developers -->
  <developers>
    <developer>
      <id>ivanbyone</id>
      <name>Ivan Boyko</name>
      <email>ivanbyone@gmail.com</email>
    </developer>
  </developers>

  <!-- We're welcome new contributors! -->
  <contributors>

  </contributors>

  <properties>
    <java.version>8</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>

    <!-- Dependencies versions -->
    <jgit.version>7.7.0.202606012155-r</jgit.version>
    <maven-plugin-api.version>3.9.16</maven-plugin-api.version>
    <maven-plugin.version>3.15.2</maven-plugin.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>${maven-plugin-api.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.maven.plugin-tools</groupId>
      <artifactId>maven-plugin-annotations</artifactId>
      <version>${maven-plugin.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.jgit</groupId>
      <artifactId>org.eclipse.jgit</artifactId>
      <version>${jgit.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-plugin-plugin</artifactId>
        <version>${maven-plugin.version}</version>
      </plugin>
    </plugins>
  </build>
</project>
